import React, { Component } from 'react';

import styles from './styles.css';

class RightView extends Component {

  render() {

    return (
      <section className={styles['layout']}>右侧控制器</section>
    );
  }
}

export default RightView;

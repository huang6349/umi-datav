import React, { Component } from 'react';

import styles from './styles.css';

class LeftView extends Component {

  render() {

    return (
      <section className={styles['layout']}>左侧选择器</section>
    );
  }
}

export default LeftView;
